models:
      - name : orders
        description : "New dbt model"
        columns:
               - name : order_id
                 description : "primary key for this table"
                 tests: 
                       - unique
                       - not_null
